import java.math.BigDecimal();
import java.math.BigInteger();

public class Sucesion4T{ 
	public Sucesion4T(int number){
		number++;
		BigDecimal res;
		BigInteger fact;
		for(int i=0;i<number;i++){
			res=res,multiply(new BigInteger(Integer.toString(i));
		}
	}
}
